{!sql
    SELECT * FROM tbl;
}

----------------------------------------------------

[
    ["sql-string", [
            ["string", "{!sql"],
            ["embedded", "\r\n    SELECT * FROM tbl;\r\n"],
            ["string", "}"]
        ]
    ]
]

----------------------------------------------------

Checks for string literals with embedded SQL.