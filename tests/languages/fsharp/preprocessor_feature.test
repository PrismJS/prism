#if foo
#else
#endif

----------------------------------------------------

[
	["preprocessor", "#if foo"],
	["preprocessor", "#else"],
	["preprocessor", "#endif"]
]

----------------------------------------------------

Checks for preprocessor directives.