$(myLicenseData)
${LANG_ENGLISH}

----------------------------------------------------

[
	["constant", "$(myLicenseData)"],
	["constant", "${LANG_ENGLISH}"]
]

----------------------------------------------------

Checks for constants.