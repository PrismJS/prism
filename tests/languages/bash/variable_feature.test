$foo
$* $@ $# $? $$ $! $0
${foo}

----------------------------------------------------

[
	["variable", "$foo"],
	["variable", "$*"],
	["variable", "$@"],
	["variable", "$#"],
	["variable", "$?"],
	["variable", "$$"],
	["variable", "$!"],
	["variable", "$0"],
	["variable", ["${foo}"]]
]

----------------------------------------------------

Checks for variables.
