/**
 * @param string $parameter a parameter
 * @param $arg2 a second parameter
 */

----------------------------------------------------

[
	"/**\r\n * ",
	["keyword", "@param"],
	["class-name", [
		["keyword", "string"]
	]],
	["parameter", "$parameter"],
	" a parameter\r\n * ",
	["keyword", "@param"],
	["parameter", "$arg2"],
	" a second parameter\r\n */"
]

----------------------------------------------------

Checks for parameters.
